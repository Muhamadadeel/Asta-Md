var create = null
var input = "string"
var user = `${input} Hi`
var sudo
var edit
var created
var error = `${created} there is an error`

function create {
    user.input = "string",
    let user.input = null
    null = "data-lost"

}
function input {
    await input = datafield
    if datafield = null
    else{
     return (error)
    }
}